'use strict';

var React = require('react');
var Home = require('./Home');

var Index = React.createClass({
    render: function () {
        return (
            <Home />
        );
    }
});

module.exports = Index;
